# OpenRouter API Configuration
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Redis Configuration
# Choose ONE of the following options:
#
# Option A (recommended on platforms like Railway): single URL, includes password and optional TLS
#   REDIS_URL=redis://default:<password>@<host>:<port>/<db>
#   - If using TLS, the scheme will be rediss://
#
# Option B (local dev / Docker Compose without auth): host/port/db, no password
#   - docker-compose.yml already sets REDISHOST=redis and REDISPORT=6379 for you
#   - for local (no Docker), use localhost and the defaults below
#
# If you are NOT using URL-style config, you can provide optional auth/TLS via:
#   REDIS_PASSWORD=your_password_here   (optional)
#   REDIS_USERNAME=default              (optional; many providers use username "default")
#   REDIS_SSL=false                     (true if your provider requires TLS on host/port style)

# Option A (commented by default)
# REDIS_URL=redis://default:password@host:6379/0

# Option B (defaults for local dev)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
# REDIS_PASSWORD=
# REDIS_USERNAME=default
# REDIS_SSL=false

# PostgreSQL Configuration
# For Docker Compose: use "postgres" as host  
# For local development: use "localhost" as host
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=loresmith
POSTGRES_USER=loresmith_user
POSTGRES_PASSWORD=somepassword

# Database URL (automatically constructed)
# For Docker Compose: postgres:5432
# For local development: localhost:5432
DATABASE_URL=postgresql+asyncpg://loresmith_user:somepassword@postgres:5432/loresmith

# Optional: Local Development Overrides
# Uncomment these if running locally without Docker:
# REDIS_HOST=localhost
# POSTGRES_HOST=localhost  
# DATABASE_URL=postgresql+asyncpg://loresmith_user:somepassword@localhost:5432/loresmith
